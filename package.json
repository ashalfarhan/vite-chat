{
  "name": "vite-chat",
  "version": "1.0.0",
  "private": true,
  "author": "ashalfarhan",
  "license": "MIT",
  "workspaces": [
    "workspaces/*"
  ],
  "scripts": {
    "server": "yarn workspace server",
    "client": "yarn workspace client",
    "dev": "concurrently -n Server,Client \"yarn server dev\"  \"yarn client dev\"",
    "build": "yarn server build && yarn client build"
  },
  "devDependencies": {
    "concurrently": "^6.3.0"
  }
}
